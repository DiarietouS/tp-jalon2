server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/restaurants
    username: postgres
    password: choupinou
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

jwt:
  secret: "tp-restaurants-inf1013-jwt-secret-key-2026"
  expiration: 24
  issuer: "auth-service"
